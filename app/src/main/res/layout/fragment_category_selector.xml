<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/primary_complement_color"
    tools:context=".CategorySelectorFragment">

    <!-- TODO: Update blank fragment layout -->
    <ListView android:id="@+id/category_list"
        android:background="@color/primary_variant"
        android:layout_margin="16dp"
        android:padding="8dp"
        android:divider="@color/primary_complement_color"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:dividerHeight="4dp"
        android:layout_weight="1"/>

    <Button android:id="@+id/confirm_category"
        android:text="@string/confirm_category"
        android:theme="@style/activatedButtonPrimary"
        android:layout_marginTop="2dp"
        android:layout_marginBottom="16dp"
        android:layout_gravity="center_horizontal"
        android:textAllCaps="false"
        android:layout_width="wrap_content"
        android:layout_weight="0"
        android:layout_height="wrap_content"/>

</LinearLayout>